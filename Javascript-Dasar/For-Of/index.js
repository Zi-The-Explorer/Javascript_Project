// For Of Di Array
const names = ["Ozi", "Zein", "Aldi"];

for(const name of names) {
    console.log(`${name}`);
};

const fullName = "Akhmad Fauzi";
for(const char of fullName) {
    console.log(char);
}