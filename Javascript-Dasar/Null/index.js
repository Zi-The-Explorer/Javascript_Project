let firstName = null;
// firstName = "Akhmad"

if (firstName === undefined) {
    console.info("UNDEFINED");
} else if (firstName === null) {
    console.info("NULL");
} else {
    console.info(`Hello ${firstName}`);
}